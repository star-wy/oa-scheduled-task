{
  "name": "netlify-auto-task",
  "version": "1.0.0",
  "description": "Netlify 定时自动化任务",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@netlify/functions": "^2.0.0",
    "@sparticuz/chromium": "112.0.0",
    "puppeteer-core": "19.11.1"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
